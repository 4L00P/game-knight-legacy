import React from 'react';
import axios from 'axios';

const Groups = () => {
// This'll be a page where groups are created and named, as well as a page where a user's created groups can be seen
// perhaps also the groups the user currently belongs to?
};

default export Groups;
